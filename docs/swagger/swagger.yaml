basePath: /api/v1
definitions:
  internal_handlers.User:
    properties:
      email:
        example: john.doe@example.com
        type: string
      id:
        example: 1
        type: integer
      name:
        example: John Doe
        type: string
    type: object
  internal_handlers_super_admin.LoginRequest:
    properties:
      email:
        type: string
      password:
        type: string
    type: object
  internal_handlers_super_admin.SuperAdminUser:
    properties:
      email:
        type: string
      first_name:
        type: string
      last_name:
        type: string
      password:
        type: string
    type: object
externalDocs:
  description: OpenAPI
  url: https://swagger.io/resources/open-api/
host: localhost:9000
info:
  contact:
    email: support@swagger.io
    name: API Support
    url: http://www.swagger.io/support
  description: This is a sample server celler server.
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  termsOfService: http://swagger.io/terms/
  title: Swagger Example API
  version: "1.0"
paths:
  /super-admin/auth/login:
    post:
      consumes:
      - application/json
      description: super admin login
      parameters:
      - description: Super Admin User Credentials
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/internal_handlers_super_admin.LoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/internal_handlers_super_admin.SuperAdminUser'
      summary: Super Admin Login
      tags:
      - super-admin
  /users:
    get:
      consumes:
      - application/json
      description: Get all userss
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/internal_handlers.User'
            type: array
      summary: Get users
      tags:
      - users
securityDefinitions:
  BasicAuth:
    type: basic
swagger: "2.0"
